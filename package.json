{
  "name": "landing-core",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    "dev": "sass --watch scss:css --style=compressed --no-source-map",
    "clean": "rimraf build",
    "build:css": "sass scss:build/css --style=compressed --no-source-map",
    "build:html": "cpy index.html build",
    "build:js": "cpy js build",
    "build:assets": "cpy assets build",
    "build": "npm run clean && npm run build:css && npm run build:html && npm run build:js && npm run build:assets"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.24",
    "cpy-cli": "^7.0.0",
    "rimraf": "^6.1.3",
    "sass": "^1.97.3"
  }
}
